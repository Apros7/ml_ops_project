# configs/config.yaml
defaults:
  - data: base
  - model/detector: yolov8n
  - model/ocr: crnn_default
  - training/detector: default
  - training/ocr: default
  - wandb_configs: default
  - _self_

seed: 42

# Override from the CLI, for example:
# uv run python -m ml_ops.train train-detector model/detector=yolov8s
# uv run python -m ml_ops.train train-ocr training/ocr=quick
